<h2>Posts about <%= @tag.name %> </h2>
<% if @posts.any? %>
<ul>
  <% @posts.each do |post| %>
    <li> <%= link_to post.title, post_path(post) %></li>
  <% end %>
</ul>
<% else %>
<h4>No posts yet!</h4>
<% end %>
